node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*
!.env.example

# public/ holds generated game data and art. It IS committed, so that hosts
# without a WoW install (Vercel and friends) can build and serve the app.
# Refresh it with `npm run data` after an MDT update.
